# --- web ---
fastapi==0.141.1
uvicorn[standard]==0.40.0
python-multipart==0.0.20
sse-starlette==3.1.1

# --- data model / storage ---
pydantic==2.13.4
pydantic-settings==2.13.0
sqlalchemy==2.0.46

# --- document intake ---
pymupdf==1.27.1
pillow==12.1.0
opencv-python-headless==5.0.0.93
pytesseract==0.3.13
numpy==2.4.1

# --- dataset generation ---
reportlab==5.0.0

# --- agents ---
ollama==0.6.2
httpx==0.28.1

# --- help desk knowledge and guardrails ---
# The help desk's answers and its refusal rules are YAML rather than Python.
# Read with safe_load only: the full loader instantiates arbitrary objects
# named in a document, which is not a thing a knowledge base should be able
# to do.
pyyaml==6.0.3

# --- observability ---
structlog==26.1.0
rich==14.3.0

# --- test ---
pytest==9.1.1
pytest-asyncio==1.3.0
